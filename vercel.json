{
  "buildCommand": "npm run build",
  "devCommand": "npm start",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/((?!api/)(?!static/).*)",
      "destination": "/index.html"
    }
  ]
}
